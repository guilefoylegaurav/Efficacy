class Event {
  String id, calendarLink, name, club, date, time, description, url;
  Event(
      {this.id,
      this.name,
      this.club,
      this.date,
      this.time,
      this.description,
      this.url,
      this.calendarLink});
}
