class EventCloud {
  String title, picture, clubId, clubName, about;
  DateTime timings;
  String id;
  EventCloud(
      {this.id,
      this.title,
      this.picture,
      this.clubId,
      this.clubName,
      this.about,
      this.timings});
}
